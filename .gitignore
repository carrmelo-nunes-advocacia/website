# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env
.env*.local

# typescript
*.tsbuildinfo

# claude code — bypass local do guard de edição + settings locais (não versionar)
.claude/.allow-infra
.claude/settings.local.json

# netlify CLI local + lockfile espúrio do deno (projeto é pnpm)
.netlify
deno.lock
